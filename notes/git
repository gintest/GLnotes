GIT 
——————————

Git init 
Git add filename 
Git commit -m “asanas”
Git Push

To check status:-
Git Status

To check out from a branch:- 
Git checkout branchname

To clone a single branch :- 
git clone --single-branch --branch <branchname> <remote-repo>

Git clone master -b branch name
git clone https://github.com/Guavus/valhalla.git -b fix/RPC-691-698
